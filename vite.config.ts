import { defineConfig } from 'vite'

export default defineConfig({
  // If you're deploying to GitHub Pages under the `portfolio` repo,
  // set the base path accordingly. Change or remove for other hosts.
  base: '/portfolio/',
})